- pc =  (程序计数器)
- *ra = return address [^1]
- sp = stack pointer
- lw = load word
- sw = store word
- *mv = move
- bnze = branch if not equal to zero
- beq = branch if equal
- *blt = branch if less to
- jal = jump and link
- ret = return
- CSR = Control and Status Register
- SBI = Supervisor Binary Interface






# Footnote
1. 「*」代表自己猜测

